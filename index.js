/*
console.log('Hello world!');


const a = 'String 1';
let b = 'String 2';
console.log('a = ', a);
console.log('b = ', b);

b = 'New string';
console.log('New b = ', b);

const c = a  + ' + ' + b;
console.log('c = ', c);

const template = `${a} - ${b}`;
console.log('template = ', template);

const str = '123';
const number1 = +str;
console.log(number1, ' ', str);

const number2 = Number(str);
console.log(number2);

const num1 = 1;
const num2 = 2;
const sumNumber = num1 + num2;
console.log(sumNumber);

function sumTwoNumber(a, b) {
    console.log(a +  b);
};

sumTwoNumber(1, 10);


function diffTwoNumber(a, b) {
    if (isNaN(a) === false) {
        if (isNaN(b) === false) {
            return a - b;
        } else {
            return 'b must have type number';
        }
    } else {
        return 'a must have type number';
    }
};

const res = diffTwoNumber(1, 2);
console.log(res);

const res1 = diffTwoNumber('sda', 2);
console.log(res1);

const res2 = diffTwoNumber(1, 'sad');
console.log(res2);
*/

/*
function auth(login) {
    const isAnon = login === 'anon';
    if (!isAnon) {
        return true;
    } else {
        return false;
    }
}

console.log(auth('anon'));
console.log(auth('neAnon'));
 */
/*
function switchEx(a) {
    switch(a) {
        case 1:
            return 'Hello';
        case 2:
            return 'Goodbye';
        default:
            return 'How are you?';
    }
}

console.log(switchEx(1));
console.log(switchEx(2));
console.log(switchEx('asd'));
 */
